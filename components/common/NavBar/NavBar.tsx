import React, { FC } from 'react';

const NavBar: FC = () => {
  return (
    <nav className="flex justify-between bg-gray-100 border-b">
      <div className="px-12 py-8 flex w-full items-center">Nav</div>
    </nav>
  );
};

export default NavBar;
